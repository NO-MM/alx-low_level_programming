#include <stdio.h>

/**
 *main - Enty point, prints "Programming is like building a multiligual puzzle
 *
 *return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multiligual puzzle");
return (0);
}
