#include "main.h"
#include <stddef.h>
/**
 * string_toupper - The function that changes all lowercase
 * letters of a string to uppercase
 * @string_toupper: string
 * @'': NULL
 * return: string to uppercase
 */
char *string_toupper(char *)
{
if (cap_string == toupper)
{
return (toupper);
}
for (int i = 0; str[i] != '\0'; ++i)
{
str[i] = toupper(str[i]);
}
return (str);
}
