#include "main.h"
/**
* main - Print a function that prints the alphabet,
* in lowercase
*
*Return: void
*/
void print_alphabet(void)
{
char lt;
for (lt = 'a'; lt <= 'z'; lt++)
{
_putchar(lt);
_putchar('\n');
}

